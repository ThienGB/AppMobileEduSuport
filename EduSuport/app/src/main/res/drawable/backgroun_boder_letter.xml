<!-- border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền cho viền -->
    <solid android:color="@android:color/transparent" />

    <!-- Đường viền -->
    <stroke
        android:width="1dp"
        android:color="@color/black_low_emp"
    />

    <!-- Góc cong của viền (nếu cần) -->
    <corners android:radius="5dp" /> <!-- Bán kính của góc cong -->

</shape>
